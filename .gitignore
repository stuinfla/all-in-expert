# Dependencies
node_modules/

# Environment (keep .env.example)
.env
.env.local
.env.*.local
!.env.example

# RuVector upstream submodule (tracked via .gitmodules, ignored from working tree)
ruvector/

# Ruflo runtime data
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Ruflo scaffolding (regenerated via ruflo init)
.claude/
.claude-flow/
.mcp.json
CLAUDE.md

# Build
dist/
build/
*.tsbuildinfo
.next/

# Data (too large for git — regenerated via pipeline)
data/captions/
data/transcripts/
data/kb/
data/profiles/

# Runtime DBs (regenerated; mid-session bytes)
*.rvf
*.rvf.lock
*.db
agentdb.rvf*
ruvector.db

# Misparsed-flag shell artifacts (do not commit)
--selector
--full-page

# QA bypass token / personal envs (never commit)
.env
.env.production
.env.local
.dev.vars

# Agentic-QE local tool state (SQLite + WAL/SHM + logs) — never commit
.agentic-qe/

# Speaker-ID working data: audio is deleted after embedding; .npz caches are large
data/audio/
data/speaker/
